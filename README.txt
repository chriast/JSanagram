This uses javascript to locate and print anagrams in a given document. 

This requires node.js and NPM installed

Before running, type the following in the console:

npm install textract

to run, type the following in console:

node anagram.js
